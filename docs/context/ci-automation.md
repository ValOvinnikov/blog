# CI & automation

> Part of the docs split described in [`docs/README.md`](../README.md).

All automation lives in `.github/workflows/` (shared pnpm/Node setup in
`.github/actions/setup`; Dependabot bumps npm + GitHub Actions weekly).
`.github/actions/setup` takes an optional `install-filter` input (default
empty, meaning an unscoped `pnpm install --frozen-lockfile` across every
workspace). When set, its value is split on whitespace into one `--filter=`
argument each, so a caller may name more than one workspace root. Six
workflows set it, all of them jobs that dispatch a single `@blog/db` script
and have no use for `apps/web`/`apps/platform`/Storybook/Playwright's
dependency trees:

| Workflow                  | Filter                        |
| ------------------------- | ----------------------------- |
| Provision Tenant          | `@blog/db...`                 |
| Deprovision Tenant        | `@blog/db...`                 |
| Recheck Tenant Owners     | `@blog/db...`                 |
| Invalidate Tenant Cache   | `@blog/db...`                 |
| Validate Tenant Documents | `@blog/db... @blog/studio...` |
| Migrate Tenant Content    | `@blog/db... @blog/studio...` |

The last two need the second filter because their `@blog/db` scripts shell out
to the Sanity CLI (`pnpm exec sanity documents validate` / `documents query`)
and to `node scripts/migrate.mjs` with `packages/studio` as the cwd. Scoping
those two to `@blog/db...` alone installs no `packages/studio/node_modules`
and the run dies on `Command "sanity" not found` — so check what a script
actually executes before narrowing a workflow's filter.

Measured on one machine against a warm pnpm store, this is worth roughly two
thirds of the install for a db-only job and roughly a third for a db+studio
one: 49.4s/1.3G unscoped, 16.0s/422M for `@blog/db...`, 31.1s/803M for
`@blog/db... @blog/studio...`. A filtered install still processes the
workspace root, so the root's `preinstall` (`guard-worktree-install.mjs`) and
`prepare` (husky) lifecycle scripts run exactly as they do unscoped.

**The pinned pnpm binary is cached, and that is load-bearing.**
`pnpm/action-setup`'s self-installer shells out to `npm install pnpm@<version>`,
and that single-package install has been observed taking upwards of seven
minutes on hosted runners — roughly thirty-five times longer than installing
this entire monorepo. Every job that sets up pnpm pays it independently, so a
slow npm registry surfaces as cancelled required checks on PRs whose diffs are
fine. `.github/actions/setup` therefore restores `~/setup-pnpm` from an
`actions/cache` entry keyed on `runner.os`/`runner.arch`/the pinned pnpm
version, and only runs `pnpm/action-setup` when that cache does not yield a
working binary of exactly the pinned version. One cold install is paid per pnpm
bump; every other job restores in seconds. Two consequences worth knowing:
a cache written on a feature branch is not visible to other branches, so the
first run after a pnpm bump pays the cold cost until `main` writes the entry
for everyone; and a restored-but-broken entry falls through to the installer
rather than failing the job, which is what the explicit version check in the
composite action is for.

**Timeout policy.** A job that uses `.github/actions/setup` gets at least
`timeout-minutes: 15`, because it pays a fixed dependency-setup cost on top of
its own work; jobs whose work genuinely takes longer (the deploy, provisioning,
dataset and content-migration jobs) keep their larger budgets. A job that
installs nothing — Actionlint, Zizmor, Dependency Review, Hooks, Board auto-sync,
and the `changes` path-filter jobs — gets `timeout-minutes: 5`. Every job
carries an explicit budget except those that call a reusable workflow, where
GitHub does not permit the key. Before this policy the budgets ranged from 5 to
15 with no stated relationship to that fixed cost, so whether a job survived
depended on which budget it happened to carry rather than on its own work: a
5-minute job was cancelled four consecutive times on a PR whose other 21 checks
were green. That job was Commitlint, since retired in favour of the
`.husky/commit-msg` hook — the floor is what stops the next short-budget job
repeating it.

**This table is a Prettier-driven merge-conflict hotspot.** Prettier repads
every column to its widest cell whenever any row's content changes width, so
two PRs that each edit a different row — merged in either order — conflict on
every other row even though their actual edits never overlap. Don't hand-align
the table to route around this; that fights Prettier instead of working with
it. To resolve: `git checkout <base> -- docs/context/ci-automation.md` (take
the base branch's file wholesale, discarding the conflict), re-apply only your
own sentence-level edit on top, run
`pnpm exec prettier --write docs/context/ci-automation.md` to repad, then
re-stage.

| Workflow                                                        | Trigger                                                                                                                               | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **CI** (`ci.yml`)                                               | every PR (any base)                                                                                                                   | The quality gates: **Type-check**, **Lint**, **Test**, **Typegen** (regenerates schema + types and fails if the committed output in `packages/config/src/sanity/generated/` is stale), **Migrations** (loads every migration + read-only dry-run against the dataset), **Document validation** (validates every existing document in the dataset against the Studio schema via `sanity documents validate`; advisory — see below, not part of the required Migrations job), **UI index** (regenerates `packages/ui/COMPONENTS.md` via `scripts/gen-ui-index.mjs` and fails if the committed index is stale, a component folder is silently unindexed / a compound slot fails to resolve, or any exported component/compound slot lacks a JSDoc description; the same job also runs `pnpm gen:ui-index:test`, fixture-based unit tests over the generator's props-resolution helpers — they need the repo's `typescript`, so they live here rather than in the install-free Hooks job; not a required check unless added to the branch ruleset), **Voice key sync** (runs `scripts/check-voice-key-sync.mjs`, which fails if the tenant-overridable "voice" copy keys hand-duplicated across `apps/platform`'s `voice-fields.ts`, `apps/web`'s `apply-voice-overrides.ts`, and `packages/db`'s `upsert-site-config.ts` (`voiceOverridesSchema`) drift out of sync; also runs its own fixture-based unit tests via `pnpm check:voice-sync:test`; not a required check unless added to the branch ruleset), **Turbo env sync** (runs `scripts/check-turbo-env-sync.mjs`, which fails if an env var declared in `apps/web`'s, `apps/platform`'s, or their transitively-imported `@blog/service`/`@blog/db`/`@blog/auth` packages' `env.ts` is missing from `turbo.json`'s `build` task `env` allowlist — turbo strict env mode silently strips an undeclared var at build time; a var legitimately read only at request/runtime can be excluded via the script's own `RUNTIME_ONLY_EXCLUDED_VARS`; also runs its own fixture-based unit tests via `pnpm check:turbo-env-sync:test`; not a required check unless added to the branch ruleset), **Revalidate tags sync** (runs `scripts/check-revalidate-tags-sync.mjs`, which fails if a tag literal passed to `isr(...)` anywhere in `packages/service/src` is missing from `apps/web`'s `REVALIDATE_TAGS` value union — an unmapped tag means the revalidation webhook silently purges nothing for that document type on publish; also runs its own fixture-based unit tests via `pnpm check:revalidate-tags-sync:test`; not a required check unless added to the branch ruleset), **Client graph** (runs `scripts/check-client-graph.mjs`, which walks every `'use client'` module's runtime import graph across `apps/*/src` and `packages/*/src` — `@blog/studio`'s mount component carries the directive too, so the two apps are not the whole surface — and fails if any chain reaches a module that imports `server-only`; that combination fails `next build` under the `react-server` condition, and nothing cheaper catches it, because every workspace aliases `@blog/<pkg>/*` straight to that package's `src/`, so a deep import into a server-only path resolves whether or not the package declares the subpath in its `exports` map; the walk stops at `'use server'` modules, which Next replaces with a network reference rather than bundling, and treats type-only imports as erased; also runs its own fixture-based unit tests via `pnpm check:client-graph:test`; not a required check unless added to the branch ruleset), **Migration index** (runs `scripts/check-migration-index-collision.mjs`, which fetches `main` and fails if a Drizzle migration index in `packages/db/migrations/meta/_journal.json` exists on both sides under a different tag — drizzle-kit numbers each migration sequentially from whatever is on `main` at `db:generate` time, so two branches generating concurrently both claim the same index and whichever merges second hits an `add/add` conflict on `meta/<idx>_snapshot.json`, which nothing surfaces until merge time; the error names both migrations and points at "Renumbering a migration that collides with `main`" in `packages/db/README.md`; also runs its own fixture-based unit tests via `pnpm check:migration-index:test`; not a required check unless added to the branch ruleset), **Required env** (runs `scripts/check-required-env.mjs` once per deployment environment as a matrix — see "Required env vars" below; not a required check unless added to the branch ruleset), **Build**. |
| **Dependency Review**                                           | every PR (any base)                                                                                                                   | Blocks dependency changes with known vulnerabilities (`fail-on-severity: high`; no license policy configured).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Zizmor** (`zizmor.yml`)                                       | every PR (any base)                                                                                                                   | Static security analysis of the workflow files themselves. Required status check.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Actionlint** (`actionlint.yml`)                               | every PR (any base)                                                                                                                   | Validates the workflow files' syntax, `${{ }}` expressions and `needs` graph, and shellchecks their `run:` blocks — the correctness half that Zizmor's security analysis does not cover. Runs the official binary, pinned by version + sha256 (no third-party action, no curl-to-shell). Required status check.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Knip** (`knip.yml`)                                           | every PR (any base)                                                                                                                   | Reports unused files, exports, and dependencies (config in `knip.json`); read-only, fails on any finding or a stale ignore rule. Required status check.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Hooks** (`hooks.yml`)                                         | every PR (any base)                                                                                                                   | Shellchecks `.claude/hooks/*.sh` and `scripts/*.sh` (outside actionlint's `run:`-block-only coverage) and runs `read-only-agent-guard.test.sh`'s, `gate-bypass-guard.test.sh`'s, `pre-bash-worktree-install-guard.test.sh`'s, `test-writer-scope-guard.test.sh`'s, and `pre-agent-gate0-guard.test.sh`'s deny/allow matrices against their respective guards, plus `vercel-ignore-affected.test.sh`'s base-resolution matrix against the shared Vercel `ignoreCommand` script. Required status check (job name `Shellcheck + guard tests`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Claude Code** (`claude.yml`)                                  | `@claude` mentions (owner-only, owner-authored threads)                                                                               | Interactive agent runs on issues/PRs. Decided advisory — it's a Claude-driven review/automation workflow, not a deterministic lint/type/test gate, so a transient failure there shouldn't block every merge. Passes `secrets.GH_PAT` into the step as the `GITHUB_PAT` env var, which is what `.mcp.json`'s `Bearer ${GITHUB_PAT}` header resolves to — without it the github MCP server 400s on a malformed Authorization header and every `mcp__github__*` tool is silently unavailable for the run. The secret cannot be called `GITHUB_PAT`: GitHub rejects Actions secret names starting with `GITHUB_`. It holds a classic PAT scoped `public_repo` + `project`; the MCP server exposes no Projects v2 tools, so board writes generally go through `gh api graphql` rather than MCP, independent of this secret. `claude_args` also carries an `--append-system-prompt` (combines with the SDK's default system prompt, the `claude_code` preset, rather than replacing it — distinct from the unused `prompt:` input, which would replace the dynamically-derived task content instead) telling the run to push and open its own PR via `gh pr create` in the same pass — without it, a run's default is to push a branch and leave a "Create PR" link for a human to click instead of opening one itself.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Deploy Development** (`deploy-development.yml`)               | **manual dispatch only** (the `push` to `main` trigger is temporarily commented out — see "Deployments are temporarily manual" below) | `turbo-ignore` change detection, anchored to the last commit the workflow completed successfully against rather than `HEAD^1`, so a cancelled run's changes are picked up by the run that superseded it (see "A cancelled dev deploy is not a lost deploy" below) → verify (type-check/lint/test/build) → auto-apply pending content migrations to the dev dataset → auto-apply pending `@blog/db` (Drizzle/Neon) schema migrations to the dev Neon branch (`migrate-db` job; `deploy-web` and `deploy-admin` depend on it) → deploy only the affected app(s) — web and/or admin panel — to the dev environment. The Sanity `migrate` job stays gated on the studio/web pair rather than on `verify`'s wider condition, so an admin-only merge doesn't run a content migration for an app that never touches Sanity. `migrate-db` opens with a guard step comparing its resolved connection host against the repo Variable `PRODUCTION_DB_HOST` and fails the job loudly on a match. `PRODUCTION_DB_HOST` must hold a **bare hostname only** (e.g. `ep-xxxx.us-east-2.aws.neon.tech`) — never a full `postgresql://` connection string; the guard also fails the job (not silently skips) if the Variable is unset or doesn't look like a bare hostname (`docs/DEPLOY.md`'s "Repo level — production-target guard for `migrate-db` (both directions)"). Every secret and Variable either migration or deploy job needs is asserted with a `:?` guard **inside** the step rather than an `if:` **on** the step (#2734, extending #2499's production-side change): `SANITY_MIGRATE_TOKEN`, `DATABASE_URL_UNPOOLED`, `VERCEL_TOKEN`, `VERCEL_ORG_ID`, and the per-app project id. A skipped step reports green, so an unset value used to leave an inert-but-green job that still satisfied the deploy jobs' `needs`; now the run fails and names the missing secret.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Deploy Production** (`deploy-production.yml`)                 | `vX.Y.Z` tag push                                                                                                                     | Verify → back up the production dataset (artifact) → gated prod content migration → back up the production Neon branch via `pg_dump` (artifact) → gated prod `@blog/db` schema migration (`migrate-db` job; `deploy-web` and `deploy-admin` depend on it) → deploy web + admin panel to production. Cutting a release = pushing a tag, so it stays under the human push gate. `migrate-db` opens with the mirror-image guard of the dev job's (#2264): it fails the job loudly if the resolved connection host **differs from** the same repo Variable `PRODUCTION_DB_HOST` — dev refuses to touch production, prod refuses to touch anything else — before the backup or migrate steps run, so a mis-set secret can no longer migrate the wrong branch (or nothing) while reporting success. Same unset/malformed-Variable guard as the dev job. `deploy-web` and `deploy-admin` apply the same principle to their Vercel credentials (#2499): each asserts **every** value it needs — `VERCEL_TOKEN`, `VERCEL_ORG_ID`, and its own project id (`VERCEL_PROJECT_ID_WEB` / `VERCEL_PROJECT_ID_PLATFORM`) — with a `:?` guard inside the deploy step, never with an `if:` on the step. A skipped step reports **green**, so a tag that shipped the web app but silently did not ship the admin panel used to look like a complete release; now the release fails and names the missing variable. #2734 extended that same treatment to the two migration jobs, where the stakes are higher: `migrate` asserts `SANITY_MIGRATE_TOKEN` and `migrate-db` asserts `DATABASE_URL_UNPOOLED` inside their steps. Only steps can be conditional, not the job, so an unset secret used to leave an inert-but-green job that still satisfied `deploy-web`/`deploy-admin`'s `needs` — shipping new code against un-migrated content and an un-migrated Postgres schema, with the dataset export and the `pg_dump` backup that would make it recoverable skipped along with the migration.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Lighthouse CI** (`lighthouse.yml`)                            | push to `main` (+ manual dispatch)                                                                                                    | Advisory Lighthouse CI budget assertions (performance ≥ 90, accessibility ≥ 95, best-practices ≥ 90, SEO ≥ 95 — `.lighthouse/budgets.json`, documented in `.lighthouse/README.md`) against **production** for `/` and one post page — not a per-PR preview (none exists or is currently tracked, `SPEC.md` §13); `development` was tried first but is unsuitable (deliberately `noindex`, plus cold-start noise), so this checks production's current health on every merge rather than that merge's own diff, until a true per-PR preview target exists. Moved off every-PR-push to merge-to-main-only — it was a structural no-op on PRs anyway. Guarded on the repo Variable `LIGHTHOUSE_URLS`; no-ops green if unset.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Playwright smoke** (`playwright-smoke.yml`)                   | push to `main` (+ manual dispatch)                                                                                                    | Minimal e2e smoke suite (`apps/web/e2e/`): home page and one post-detail page (discovered dynamically from a live link, no hardcoded slug) each render 200 with zero console errors, run against `SMOKE_URL`. **Currently a no-op green** for the same reason as Lighthouse CI above — no PR preview deploys yet, and no per-PR preview mechanism is currently tracked, so the job is guarded on the repo Variable `SMOKE_URL`. Moved off every-PR-push to merge-to-main-only, same reasoning as Lighthouse CI.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Refresh Dev Dataset** (`refresh-dev-dataset.yml`)             | manual `workflow_dispatch` only                                                                                                       | Replaces the `development` Sanity dataset with a fresh, published-only copy of `production` (cross-project export→import — dev and prod are separate Sanity projects). Runs in a dedicated `dataset-refresh` GitHub Environment (needs both projects' credentials at once). Never automatic, never part of a deploy — run manually only after that release's production migrations complete (see `docs/DEPLOY.md`'s "Refreshing development from production"). The target dataset name is hardcoded in the script, not env-derived, so a misconfigured environment can't reverse the copy direction.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Provision Tenant** (`provision-tenant.yml`)                   | manual `workflow_dispatch` only                                                                                                       | Provisions one tenant end to end for a `tenants` row already inserted by the admin app's "Add tenant" wizard Server Action (via the GitHub API): if the row is a previously-deprovisioned tenant being re-provisioned, first un-archives its existing Sanity project (`isDisabledByUser: false`) so the reactivated tenant's content API works again — then creates its Sanity project/dataset/CORS entry, seeds a fixed starter content template, mints and persists its read-only Sanity token, maps its custom domain onto the _shared_ `apps/web` Vercel project, and creates a Sanity webhook on its project pointing at that same shared `apps/web` app's `/api/revalidate` endpoint (signed with `SANITY_REVALIDATE_SECRET`) so publishing content there triggers cache revalidation. Runs in the GitHub Environment named by the required `environment` input (`development`/`production`, default `production`) — every credential the job needs, including the tenant registry connection string (`secrets.TENANT_REGISTRY_DATABASE_URL`, kept separate from `DATABASE_URL_UNPOOLED`, which `deploy-production.yml`'s `migrate-db` job reads instead), resolves from that same Environment, so a dispatch never mixes one environment's tenant registry with another's Sanity/Vercel credentials. Every step is independently idempotent — re-dispatching the same `tenantId` resumes at the first incomplete step rather than repeating finished work — and writes each step's status directly to Postgres (via `@blog/db`'s `updateProvisioningStep`, no HTTP hop to `apps/platform`), both on success and failure. Each of those writes also stamps that step's `updatedAt`, and the run as a whole records a `run` object (`startedAt`/`finishedAt`/`registry`/`workflowRunUrl`) alongside the step keys in the same `provisioning_steps` jsonb payload — `registry` being the dispatched `environment` input, passed in as `TENANT_REGISTRY_ENVIRONMENT`, and the workflow-run URL built from Actions' own default `GITHUB_SERVER_URL`/`GITHUB_REPOSITORY`/`GITHUB_RUN_ID` (which need no `env:` entry). `apps/platform`'s provisioning page renders both, so a failed run's timeline is readable without opening this workflow's log. Logic lives in `packages/db/scripts/provision-tenant/` (a `tsx --conditions=react-server` Node script, same execution pattern as `scripts/seed-tenant.ts`), not in the workflow YAML itself. Deliberately manually-triggered only, same posture as Refresh Dev Dataset — it fails loudly rather than no-op-green when a required secret is missing. Accepts optional `adminAppBaseUrl` and `tenantSanityDataset` inputs that override `ADMIN_APP_BASE_URL`/`TENANT_SANITY_DATASET` for that one dispatch (e.g. a local tunnel URL, or `development`, for manual testing); left blank, each falls back to the dispatched environment's own value — the real dispatch from `apps/platform` never sets either input, so it's unaffected. Once core provisioning finishes, also runs the same post-provisioning owner-elevation check Recheck Tenant Owners' sweep runs (`elevateTenantOwner`) and, via the shared `notifyOwnerElevationOutcome` helper (`packages/db/scripts/provision-tenant/lib/notify-owner-elevation-outcome.ts`), reports an operator-alert event to `apps/platform`'s `/api/internal/operator-alert` endpoint through the optional `ADMIN_APP_BASE_URL`/`OPERATOR_ALERT_SECRET` job env vars if the outcome is notifiable (`STALLED`/`AMBIGUOUS_MEMBERSHIP`) — the platform app resolves every `ADMIN_ROLE.SUPERADMIN` admin and sends the email; see the Recheck Tenant Owners row below for the shared de-dup mechanism. A second job, gated on the first succeeding, calls Validate Tenant Documents (below) as a reusable workflow (`workflow_call`) scoped to just the freshly provisioned tenant, so a tenant seeded already-invalid by `starter-content.ts` is caught immediately rather than waiting for that workflow's next scheduled sweep.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Deprovision Tenant** (`deprovision-tenant.yml`)               | manual `workflow_dispatch` only                                                                                                       | Reverses Provision Tenant for one tenant: removes its custom domain from the shared `apps/web` Vercel project, archives its Sanity project by setting `isDisabledByUser` — which stops its subscription and blocks API/CDN access, and unlike deleting a project does not require an org billing permission this token deliberately lacks, so the project and its dataset survive for a later permanent tenant delete — clears the persisted Sanity credentials and other provisioning-artifact columns on the `tenants` row, then archives the row by stamping `deprovisionedAt` — never a hard delete, so the tenant's history and audit trail survive teardown. Its domain stays claimed in `tenant_domains` too; only a permanent tenant delete cascades that row away and frees the domain for reuse. Finally it POSTs the archived tenant's id to `apps/web`'s `/api/revalidate-site-config`, purging that tenant's cache tags so the archived site stops being served from the prerender cache instead of waiting out its fallback window; this step needs `vars.WEB_APP_URL` and `secrets.SITE_CONFIG_REVALIDATE_SECRET` from the same Environment, and — unlike every other credential the job reads — they are deliberately **not** validated up front, so a missing value can never block the destructive teardown steps that precede it. It is also the one step that is **not** idempotent-on-retry: the run-level `deprovisionedAt` guard short-circuits an already-archived tenant, so a re-run dispatched specifically to retry a failed invalidation returns "already deprovisioned" without reaching it — recovering from that failure means dispatching **Invalidate Tenant Cache** (`invalidate-tenant-cache.yml`, below) instead, which re-runs only this one step against the tenant id and is unaffected by the `deprovisionedAt` guard. Destructive, so two safety gates apply before any deletion: a `confirm` input that must match the tenant's live name exactly, and a `dryRun` input defaulting to `true` (an operator must explicitly set it to `false` to delete anything). Runs in the GitHub Environment named by the required `environment` input, same as Provision Tenant — every credential the job needs, including the tenant registry connection string (`secrets.TENANT_REGISTRY_DATABASE_URL`), resolves from that same Environment (see the Provision Tenant row above). Every step is independently idempotent (404-tolerant deletes, column-presence checks) — a retry after a partial failure resumes safely. Archiving also records one `DEPROVISIONED`/`TENANT` row in `audit_events` — the outcome half of the pair the platform app opens with `DEPROVISION_REQUESTED` when it dispatches this workflow — attributed to the dispatching GitHub user (`actor_id` `github:<login>`) from the `GITHUB_ACTOR`/`GITHUB_RUN_ID` defaults Actions sets on every step — a dry run or an already-archived tenant records nothing, and a failed audit write is logged and swallowed rather than failing the step whose destructive work already succeeded. Logic lives in `packages/db/scripts/deprovision-tenant/`, same execution pattern as `provision-tenant/`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Invalidate Tenant Cache** (`invalidate-tenant-cache.yml`)     | manual `workflow_dispatch` only                                                                                                       | Retries just Deprovision Tenant's final step for one tenant id — POSTs it to `apps/web`'s `/api/revalidate-site-config` to purge its cached pages, without touching any domain, Sanity project, token, or the `tenants` row itself. This is the recovery path for a failed invalidation the Deprovision Tenant row above describes: the `deprovisionedAt` guard blocks a normal re-dispatch of that workflow from ever reaching the step again once the tenant is archived, so this workflow calls it directly instead, independent of that guard. Safe against both an already-archived and a still-active tenant, and safe to run repeatedly — the endpoint only purges cache tags, so no `confirm`/`dryRun` gate is needed. Runs in the GitHub Environment named by the required `environment` input, same as the other two tenant-lifecycle workflows, resolving the tenant registry connection string and `WEB_APP_URL`/`SITE_CONFIG_REVALIDATE_SECRET` from that Environment; fails loudly, before any network call, if either is absent. Logic lives in `packages/db/scripts/invalidate-tenant-cache/`, which calls the same `invalidateTenantCache` step function `deprovision-tenant/` uses.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Recheck Tenant Owners** (`recheck-tenant-owners.yml`)         | `schedule:` every 6 hours (`0 */6 * * *`) + manual `workflow_dispatch`                                                                | Sweeps every fully core-provisioned, active tenant (`status = ACTIVE`, `provisioningStatus = READY`) and re-checks its Sanity project ACL, promoting the owner from `viewer` to `administrator` once they've accepted their invite (`elevateTenantOwner`, the same function Provision Tenant's own single post-provisioning check calls). Provision Tenant only checks once, right after core provisioning; a human owner routinely accepts well after that single check, so this scheduled sweep is the recurring re-check needed to detect a stalled acceptance without waiting on an unrelated deploy. The 6-hour cadence gives ~12 checks within `elevateTenantOwner`'s existing 3-day stall threshold (`OWNER_ACCEPTANCE_STALL_THRESHOLD_MS`) — good resolution for surfacing a stall promptly, while also elevating a genuinely-accepted owner within hours rather than up to a day later. One candidate's failure never aborts the sweep for the rest (per-tenant try/catch), and re-checking a settled (already-`administrator`) owner is a cheap no-op single ACL read — repeated checks stay cheap even for a tenant that never accepts. Needs only `SANITY_MANAGEMENT_TOKEN` and the tenant registry connection string — no Vercel credentials, this workflow never touches Vercel. Like Provision Tenant/Deprovision Tenant, its job binds its GitHub Environment to its own optional `environment` input (`development`/`production`, default `production`), so the registry connection string and every other credential resolve from the Environment that dispatch names rather than a fixed one. Unlike those two it also runs on a `schedule:`, and a scheduled run carries no `inputs` context at all, so it binds that input with a `'production'` fallback: every cron sweep targets `production`, and only a manual dispatch reaches `development`. Logic lives in `packages/db/scripts/recheck-tenant-owners/`, same execution pattern as `provision-tenant/`/`deprovision-tenant/`. Each check's outcome is persisted to the tenant's `OWNER_ELEVATION` provisioning step (`reportOwnerElevationOutcome`), so the platform admin panel can render it. The job's `ADMIN_APP_BASE_URL`/`OPERATOR_ALERT_SECRET` (both optional) POST an operator-alert event to `apps/platform`'s `/api/internal/operator-alert` endpoint, via the shared `notifyOwnerElevationOutcome` helper (`packages/db/scripts/provision-tenant/lib/notify-owner-elevation-outcome.ts`), when a tenant's outcome is `STALLED`/`AMBIGUOUS_MEMBERSHIP` — the platform app resolves every `ADMIN_ROLE.SUPERADMIN` admin and sends the email. De-duplication keys on the `tenants.lastNotifiedOwnerElevationOutcome` column — the outcome last actually communicated, written by this same helper right after a successful send — rather than the most recently _observed_ outcome, so a same-day sweep never re-sends for an outcome unchanged since the last notification, whether that notification happened on an earlier sweep or during Provision Tenant's own post-provisioning check (see that row above). Either unset skips only the notification, never the sweep itself (`notifyOperatorsOfOwnerElevationOutcome`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Validate Tenant Documents** (`validate-tenant-documents.yml`) | `schedule:` weekly (`0 6 * * 1`) + manual `workflow_dispatch` + `workflow_call`                                                       | Runs `sanity documents validate` (read-only) against every `status = ACTIVE`, non-deprovisioned tenant's own Sanity project/dataset — the console reference project is validated separately by `ci.yml`'s advisory Document validation job, which this workflow never touches. Each per-tenant CLI invocation overrides `SANITY_STUDIO_PROJECT_ID`/`SANITY_STUDIO_DATASET`/`SANITY_AUTH_TOKEN` (the tenant's own persisted **read** token, decrypted via `getTenantSanityCredentials` — no write credential is used or needed), following the same env-override shape `refresh-dev-dataset.mjs` uses for its cross-project export/import, since `packages/studio/sanity.cli.ts` resolves those vars at config-load time before any CLI flag is parsed. Always advisory: this job's own exit code reflects only a systemic failure (the CLI/network failing, or a findings-store write failing) — an expected outcome (some/all documents invalid) is reported, never treated as a failure, so a tenant seeded already-invalid by `starter-content.ts` is surfaced, not blocked. One tenant's failure never aborts the sweep for the rest (per-tenant try/catch, `packages/db/scripts/validate-tenant-documents/run.ts`). Every invalid-document result is persisted as one `findings` row per tenant (`source: DOCUMENT_VALIDATION`, `kind: SCHEMA_VALIDATION_ERROR`) via `db.queries.findings.openFinding`, rendered on that tenant's detail page and the platform-wide findings list in `apps/platform` — no dedicated migration or admin-panel work was needed, since the shared findings store already existed. A clean re-validation resolves any previously open finding for that tenant (`resolveFinding`). The optional `ADMIN_APP_BASE_URL`/`OPERATOR_ALERT_SECRET` job env vars POST an operator-alert event to `apps/platform`'s `/api/internal/operator-alert` endpoint, via `packages/db/scripts/validate-tenant-documents/lib/notify-operators.ts`, only on `openFinding`'s `isNewlyOpened: true` — a tenant's first transition into a failing state — never on a sweep that finds the same tenant still failing, so a known-bad tenant doesn't re-spam every run; the platform app resolves every `ADMIN_ROLE.SUPERADMIN` admin and sends the email. Three triggers: the weekly cron (every in-scope tenant), a manual `workflow_dispatch` optionally scoped to one tenant via a `tenantId` input (blank sweeps every in-scope tenant), and a `workflow_call` Provision Tenant's own second job invokes right after provisioning succeeds, scoped to that one freshly provisioned tenant (see the Provision Tenant row above). Like the other tenant-lifecycle workflows, its job binds its GitHub Environment to `${{ inputs.environment \|\| 'production' }}` — the `\|\| 'production'` fallback is load-bearing for the cron trigger, whose `inputs` context is null. Needs only the tenant registry connection string and `TENANT_TOKEN_ENCRYPTION_KEY` — no `SANITY_MANAGEMENT_TOKEN`, unlike Provision Tenant/Recheck Tenant Owners, since this workflow only ever reads via each tenant's own already-persisted token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Migrate Tenant Content** (`migrate-tenant-content.yml`)       | push to `main` (dev only, gated on the studio/web change pair) + manual `workflow_dispatch` + `workflow_call`                         | Applies pending Sanity **content** migrations to every `status = ACTIVE`, non-deprovisioned tenant's own Sanity project — closing the gap where `deploy-development.yml`/`deploy-production.yml`'s `migrate` jobs reached only the single console project, leaving every tenant dataset silently drifted from the schema on any rename/removal/restructure (purely additive schema changes were never affected). Logic lives in `packages/db/scripts/migrate-tenant-content/`, not the workflow YAML, same as the other tenant-lifecycle scripts. Each per-tenant invocation shells `packages/studio/scripts/migrate.mjs deploy --yes` with `SANITY_STUDIO_PROJECT_ID`/`SANITY_STUDIO_DATASET`/`SANITY_AUTH_TOKEN` overridden to that tenant — the token being the tenant's own persisted **write** credential (`getTenantSanityWriteCredentials`, decrypted per run), **not** an organisation-scoped one, so blast radius stays a single tenant per credential; no org-wide content-write token exists in this repo anyway (`SANITY_MANAGEMENT_TOKEN` is Management-API/ACL scope, `SANITY_MIGRATE_TOKEN` is console-project scoped). **Empty-ledger tenants are backfilled, never replayed:** `migrate.mjs` keeps its `migrationState` ledger per dataset, so a tenant provisioned before this workflow existed has an empty ledger and a naive run would execute every historical migration against it — but such tenants were seeded from starter content at the then-current schema, so replaying migrations written for older shapes ranges from no-op to destructive. `isTenantMigrationLedgerEmpty` is therefore checked before **every** tenant's deploy call and switches it to `--backfill` (mark applied without executing); that is structural rather than an opt-in flag, so no code path reaches the deploy without resolving it first. Idempotent and resumable by construction, the ledger being per-dataset — a tenant added later just catches up on the next run. One tenant's failure never aborts the sweep (per-tenant try/catch), and the job's exit code reflects only a **systemic** failure (CLI/network/store), never an expected per-tenant outcome. **Production is deliberately NOT tag-triggered.** Unlike `migrate:deploy`, which fans out to the console project on a `vX.Y.Z` tag, this workflow requires an explicit `workflow_dispatch` for production — still bound to `environment: production`, so that approval gate applies _in addition to_ the dispatch, not instead of it. The divergence is intentional, not an oversight to be tidied into consistency: one run mutates _customer_ content across N projects at once, and cutting a release should not implicitly rewrite every tenant's documents. Binds its Environment to `${{ inputs.environment \|\| 'production' }}` like the other tenant workflows (the fallback is load-bearing for the non-dispatch trigger), and needs the tenant registry connection string plus `TENANT_TOKEN_ENCRYPTION_KEY`. `workflow_call` is exposed for a future single-tenant caller, but `provision-tenant.yml` is **not** wired to it today — a freshly provisioned tenant's empty ledger is safely backfilled on the next run regardless.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

| **Board Auto-Sync** (`board-auto-sync.yml`) | `create` (branch) + `pull_request` (opened, reopened) | Turns cloud-session branch/PR evidence into board status writes, closing the gap in `CLAUDE.md`'s cloud-session protocol where status otherwise waits on a local session dispatching `board-keeper`. A pushed branch matching `<type>/<n>-<slug>`, the legacy `issue/<n>-*` form, or the GitHub Actions `@claude` cloud shape `claude/issue-<n>-<timestamp>` moves issue `#n` to **In Progress**; an opened/reopened PR whose body carries `Closes #n`/`Fixes #n`/`Resolves #n` (falling back to the branch name) moves it to **Code Review** — both forward-only, mirroring `board-keeper.md` Step 4's safe-transition list, and both re-verified with an independent read after the write (the same Projects v2 silent-write-failure mode `board-keeper.md` documents). Shared logic lives in `.github/scripts/sync-board-status.sh`. Uses the `GH_PAT` secret (see the Claude Code row above) since the default `GITHUB_TOKEN` cannot write Projects v2; the workflow fails loudly, before any API call, if that secret is absent. Gated to `github.actor == 'ValOvinnikov'` on both triggers, same posture as `claude.yml` — without it, a PR body's `Closes #n` is attacker-controllable input any contributor to this public repo could use to move an arbitrary issue on the board. |

**Required status checks on `main`:** Type-check, Lint, Test, Typegen, Build,
Migrations, Dependency Review, Zizmor, Actionlint, Knip, and Hooks
(`Shellcheck + guard tests`). Everything else (Claude Code, Document
validation, Lighthouse CI, Playwright smoke) stays advisory: a red result
there does not currently block a merge.
Required workflows run on every PR without path filters **and without
base-branch filters**, on purpose — a workflow skipped by either leaves its
required checks pending forever, so it blocks the PR instead of reporting on
it (see the comment in `ci.yml`).

**The base-branch half of that is what makes stacked PRs viable.** Every
workflow above that carries a required check — `ci.yml`, `knip.yml`,
`dependency-review.yml`, `zizmor.yml`, `actionlint.yml` and `hooks.yml` —
declares a bare `pull_request:` trigger, so a PR opened against
any branch (the normal shape of a stacked PR) still runs all eleven. A
`branches: [main]` filter here would strand such a PR permanently: it would
run none of the required jobs, and retargeting would not rescue it either,
since GitHub's retarget on base merge fires `edited` while the workflows
listen for `opened`/`synchronize`/`reopened`.

**If you are tempted to narrow these triggers again, note the scope trap:**
the scoping is declared independently in each of the six workflow files, so
touching only `ci.yml` moves six of the eleven required checks and leaves a PR
just as blocked on the other five. All six change together or not at all.
The cost of the current setting is that the full suite runs against arbitrary
base branches; that is compute only, since these are `pull_request` triggers
rather than `pull_request_target` and the base branch does not change what
permissions or secrets a run receives.

Document validation is a separate job inside `ci.yml` from the required
Migrations job precisely so a routine drift in an existing live document
(independent of any given PR's diff) can't fail a required check — same
reasoning as Lighthouse CI / Playwright smoke below.

**Document validation runs clean, including on `migrationState`.**
`migrationState` (`packages/studio/scripts/migrate.mjs`'s single fixed-`_id`
migration ledger document) has a declared Studio schema type
(`packages/studio/src/schema-types/documents/system/migration-state.ts`) so
`sanity documents validate` recognizes it, while `sanity.config.ts` disables
every Studio document action and excludes it from the new-document menu —
it's still never edited via the Studio UI. This supersedes an earlier
decision (#1580) to leave it unrecognized as an accepted warning; #1882
revisited that call and declared the type instead, since the job being
advisory doesn't make an avoidable warning worth carrying indefinitely.

**Lighthouse CI and Playwright smoke stay advisory by design, and both run
only on push to `main`, not on every PR push.** Playwright smoke is still a
structural no-op — it depends on a preview/smoke URL this repo doesn't
produce yet (no PR preview deploys, per `SPEC.md` §13) — so promoting it
stays a deliberate follow-up once `SMOKE_URL` is configured and it can
genuinely fail. Lighthouse CI targets production and **can** genuinely fail
— it stays advisory anyway because it checks production's current health
rather than the merge's own diff (no per-PR preview target exists or is
currently tracked), which makes a blocking failure there potentially
unrelated to the change that triggered it. Both were moved off
every-PR-push to merge-to-main-only: neither ever gated on the PR's own
diff (same production/live-URL target regardless of which PR pushed), so a
PR-push trigger only paid the run cost without adding signal a post-merge
run doesn't already give.

**A cancelled dev deploy is not a lost deploy.** Deploy Development's
`changes`, `verify` and `deploy-*` jobs each keep `cancel-in-progress: true`,
so a newer merge still supersedes an in-flight build — "latest merge wins".
That is only lossless because the `changes` job anchors `turbo-ignore` to the
head commit of the **last successful run of the workflow on `main`**, not to
`HEAD^1`. With a one-merge-wide `HEAD^1` window, a merge whose run was
cancelled fell outside every window that followed and was silently never
deployed — the pipeline went green having shipped nothing, and whether the
change eventually landed depended on chance (a later merge happening to touch
the same turbo graph). Anchoring to the last success stores no state of its
own — GitHub's run history is the state — and self-heals: a cancelled or
failed run never becomes the anchor, so the next merge re-evaluates every
commit back to the one the workflow last got all the way through. Note the
anchor is a **diff boundary, not a claim that something shipped**: a docs-only
merge, and a run whose deploy step is a guarded no-op because its Vercel token
isn't configured yet, both conclude `success` and both advance it. That stays
correct — the anchor's only job is to bound the window, so once a token is
configured the next real run inherits the whole accumulated window rather than
one merge of it. The
`changes` job therefore needs `actions: read` and a full-history checkout, and
logs a `::notice::` naming the anchor whenever the window is wider than one
merge (i.e. whenever it is covering for an earlier cancelled run). Queueing
the deploy jobs instead (`cancel-in-progress: false`, as `migrate` and
`migrate-db` do for their own reason — never interrupt a migration
mid-transaction) would **not** have fixed this: GitHub cancels an older
_pending_ run when a newer one queues, so a middle merge can still be dropped
even though every deploy that does start runs to completion. The trade-off
taken is a wider detection window — after a cancellation the next run may
rebuild an app whose own diff didn't require it — in exchange for never
losing a deploy. Deploy Production is unaffected: it never cancels
(workflow-level `cancel-in-progress: false`) and a tag deploys both apps
unconditionally, so it has no detection window to widen. The one case that
still needs a human is a run cancelled with no merge behind it (a manual
cancel on the newest commit): nothing is queued to heal it, so re-run the
workflow or dispatch it manually. This mirrors the comparison-commit
reasoning the Storybook `ignoreCommand` script already uses below.

**`apps/web`'s Storybook coverage is a hosted Vercel deployment, not a CI
workflow** — mirroring `packages/ui`'s existing `ui-library` project
(neither appears in the table above; a hosted Vercel deployment isn't a
GitHub Actions workflow). A second Vercel project, rooted at the repo root
(`vercel.json`, scoped via turbo to `storybook:build --filter=web`), builds
and previews `apps/web`'s own stories — see
[`docs/DEPLOY.md`](../DEPLOY.md)'s Storybook section for the setup.

Both Storybook projects share one `ignoreCommand` script,
`scripts/vercel-ignore-affected.sh`, in **two modes**.

`ui-library` uses **affected mode**
(`vercel-ignore-affected.sh <package> <watched-paths…>`): a push builds it
only when turbo sees `@blog/ui` as **affected**, _or_ when one of its watched
deploy-config paths changed — its own `vercel.json` and the shared ignore
script itself. Watching the config files means a deploy-config change (which
turbo can't see as "affecting" the package) still gets validated by a real
build instead of being silently skipped; watching the script means a change
to the skip logic self-verifies.

`web-ui-library` uses **allowlist mode**
(`vercel-ignore-affected.sh --paths <trigger-paths…>`), which drops turbo and
builds only when one of the named paths changed: the two deploy-config paths
above, plus `apps/web/.storybook`, `apps/web/**/*.stories.tsx`, `packages/ui`
and `configs/tailwind`. Affected mode was a poor fit here because the project
is scoped to the `web` package, which nearly every commit touches — it
rebuilt on ~41% of all pushes (252 of 609 over twelve days) while the stories
themselves had not moved, and Storybook builds were the single largest
consumer of the account's Vercel build-CPU allowance. Naming the real inputs
confines rebuilds to commits that can actually change the output. The trade
is a deployed Storybook that lags a plain component edit until a story,
`@blog/ui`, or the Tailwind preset moves — acceptable for a browsable
reference that is run locally (`pnpm storybook`) during development. Note
that a dependency bump alone (`pnpm-lock.yaml`) is deliberately **not** a
trigger; bump Storybook or Vite and the deployed build stays stale until one
of the listed paths moves.

Everything outside a project's triggers skips, so an unrelated PR builds
neither Storybook.

## Required env vars

Every env var in this repo's `env.ts` modules is either required in the zod
schema or `.optional()`, and `.optional()` has to carry two different meanings:
a feature that is legitimately off, and a feature that is silently dead because
nobody provisioned its secret. `createEnv` validates eagerly at import time and
one env module is imported by every entry point, so marking a route-scoped
secret required in the schema would turn one webhook's missing secret into a
whole-app outage — optional-at-boot with a loud guard at the point of use is
correct, and it is what leaves "required in production" stated nowhere a
machine can check.

The **Required env** job closes that gap. Every `.optional()` var carries one
marker comment directly above its declaration:

```ts
// @env-required: development, production
SITE_CONFIG_REVALIDATE_SECRET: z.string().min(1).optional(),

// @env-optional
WEB_ANALYTICS_ENABLED: z.enum(['true', 'false']).optional(),
```

`scripts/check-required-env.mjs` parses those markers out of the `env.ts`
modules as a TypeScript AST, exactly as `check-turbo-env-sync.mjs` parses the
same files. It does two things:

1. **Classification coverage** — an `.optional()` var with no marker fails the
   run. This half is source-only, needs no credentials, runs anywhere
   (`pnpm check:required-env` locally), and is always blocking. It is what stops
   a newly added optional var from merging unclassified, and it is why the
   classification lives beside the declaration rather than in a separate
   manifest that would drift.
2. **Presence verification** — for each deployment environment, the vars marked
   required for it must actually exist there.

**No token is involved.** The job declares `environment: <name>`, so the
`secrets` and `vars` contexts resolve to that environment's own entries merged
over the repo-level ones. Listing environment secrets through the API instead
would need repo-admin scope, which neither the default `GITHUB_TOKEN` nor the
`public_repo + project` `GH_PAT` has.

**No secret value reaches the runner.** The step passes one boolean per
required var — `REQUIRED_ENV_PRESENT_<NAME>: ${{ secrets.<NAME> != '' }}` — so
the comparison happens in GitHub's expression engine and only `true`/`false`
is ever set as an environment variable. Passing `toJSON(secrets)` would save
maintaining that list and would still let the script read names only, but it
injects every secret into the runner's environment; zizmor flags it as
`overprovisioned-secrets`, correctly.

The cost is a list of var names in the workflow that could drift from the
markers. It cannot drift silently: a var marked required with no matching
`REQUIRED_ENV_PRESENT_` input fails the run and prints the exact line to add,
and `pnpm check:required-env:test` asserts the same thing against `ci.yml`, so
the drift surfaces locally too.

Two consequences of the `environment:` approach worth knowing: the `secrets`
context merges repo-level secrets in, so a repo-level secret counts as present
for every environment (which is what a workflow actually resolves at runtime),
and a job carrying `environment:` shows up in that environment's deployment
history.

`development` is blocking; `production` warns and exits 0 while its known gaps
are open, so the check does not land red. That policy is
`BLOCKING_ENVIRONMENTS` in the script, not workflow YAML — move `production`
into it in whichever PR closes those gaps.

**Scope limit.** A GitHub Environment holds the vars CI workflows consume plus
the ones mirrored for deploy. A var read only by the running app —
`VERCEL_API_TOKEN`, `BLOB_READ_WRITE_TOKEN`, `ANTHROPIC_API_KEY` — is
provisioned in the Vercel project and never appears in a GitHub Environment at
all, so only vars provisioned in a GitHub Environment may be marked required
today. Extending the check to the Vercel side is tracked separately; see
[`environment-variables.md`](environment-variables.md).

## Deployments are temporarily manual (2026-09-04)

**Nothing deploys automatically right now.** Every automatic deployment
trigger is switched off to stay inside the Vercel metered-usage allowance
during a low-development stretch. Restoring them is tracked in #2648:

| Surface                       | How it is disabled                                                   | How to deploy now                                               |
| ----------------------------- | -------------------------------------------------------------------- | --------------------------------------------------------------- |
| Dev web app + dev admin panel | `deploy-development.yml`'s `push: branches: [main]` is commented out | `gh workflow run deploy-development.yml`, or the Actions tab    |
| `web-ui-library` Storybook    | `git.deploymentEnabled: false` in the root `vercel.json`             | Vercel dashboard → the project → **Redeploy**, or a deploy hook |
| `ui-library` Storybook        | `git.deploymentEnabled: false` in `packages/ui/vercel.json`          | same                                                            |

`deploymentEnabled: false` blocks only **Git-triggered** builds — dashboard
redeploys and deploy hooks still work, which is what keeps manual deploys
available. It is the same mechanism `apps/web` and `apps/platform` have always
used (#445); the two Storybook projects were simply the ones still on Vercel's
native Git integration.

**Why deployment count, not build time, was the problem.** Every deployment
pays roughly five seconds of build container just to clone the repo and run
`ignoreCommand` — even when that command decides to skip. At ~44 merges a day
across four Git-connected projects, the skips alone outweighed the real
builds, which is why narrowing `web-ui-library`'s triggers (above) cut its
build rate from 38% of deployments to 8% without reducing spend. Only removing
the deployments themselves moves the number.

**Two consequences to expect while this is in force.** The dev environment
drifts from `main`, so a manual dispatch is the first thing to run when
picking work back up. And Sanity content migrations plus `@blog/db` schema
migrations ride in `deploy-development.yml`, so a merged schema change leaves
the dev dataset and dev Neon branch unmigrated until that dispatch — the one
effect here that is silent rather than visible.

**When restoring automatic deploys, uncomment the trigger — do not add an
`if:` guard instead.** The `changes` job anchors to the `head_sha` of the last
_successful_ run of the workflow, so a run that succeeds with its deploy jobs
skipped advances the anchor past commits that were never deployed, and the
next real run silently skips them. A workflow that never runs leaves the
anchor parked, which is exactly what lets a later manual run cover every
commit since.

**Which commit the diff is taken against** decides all of the above, and it
depends on the situation:

- `VERCEL_GIT_PREVIOUS_SHA` when Vercel supplies one — the last **successful**
  deployment for that project and branch.
- Otherwise the merge-base with `main`, fetched by explicit URL (Vercel's build
  clone has no `origin` remote).
- Otherwise `HEAD^1`. This last step is what makes a **`main` build** work: on
  `main`, `FETCH_HEAD` is `main`, so the merge-base resolves to `HEAD` itself,
  and a commit diffed against itself shows nothing affected. Before this rule
  existed, a project that had never deployed successfully skipped every single
  `main` build — which never produced a deployment, so `VERCEL_GIT_PREVIOUS_SHA`
  stayed empty and the next merge repeated it. `web-ui-library` sat in exactly that
  deadlock, showing "No Production Deployment", until it was broken.

In **affected mode**, a change touching **only**
`packages/config/src/types/**` or `packages/config/src/sanity/generated/**`
skips regardless of affectedness. Turbo marks every dependent affected by any
`packages/config` change, but both `storybook:build` scripts are a bare
`storybook build`, which transpiles without type-checking — so type-only files
can neither reach the output nor break the build. A type-only file alongside
any other change still builds. Allowlist mode needs no such carve-out:
`packages/config` is not a trigger path, so it skips by construction.

`scripts/vercel-ignore-affected.test.sh` pins all of this (see the Hooks row
above); the script has no failing signal of its own, so a regression here is
invisible until someone notices a project has stopped deploying.

One-time environment setup (datasets, tokens, Vercel projects, secrets,
webhooks, CORS) is human-gated console work — see [`docs/DEPLOY.md`](../DEPLOY.md)
and `SPEC.md` §13. Manual `sanity deploy` / Vercel deploys are never run by
hand (agent permissions deny them; the pipeline owns deploys).
