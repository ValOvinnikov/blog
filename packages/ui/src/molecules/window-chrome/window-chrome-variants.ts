import { tv } from '@blog/ui/lib/styling';

export const windowChromeVariants = tv({
  base: [
    'block',
    'rounded-md border border-border bg-surface',
    'overflow-hidden shadow-sm',
  ],
});
