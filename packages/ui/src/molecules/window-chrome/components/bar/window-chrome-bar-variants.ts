import { tv } from '@blog/ui/lib/styling';

export const windowChromeBarVariants = tv({
  base: [
    'flex flex-wrap items-center gap-1.5',
    'border-b border-border bg-surface-2',
    'px-3.5 py-2',
    'font-mono text-meta text-text',
  ],
});
