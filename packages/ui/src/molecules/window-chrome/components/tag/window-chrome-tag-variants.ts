import { tv } from '@blog/ui/lib/styling';

export const windowChromeTagVariants = tv({
  base: [
    'ml-auto inline-flex items-center whitespace-nowrap',
    'rounded-full border border-border',
    'px-2.5 py-0.5',
    'font-mono text-label text-text-muted uppercase tracking-eyebrow',
  ],
});
