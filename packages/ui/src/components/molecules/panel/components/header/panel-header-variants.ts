import { tv } from '@blog/ui/lib/styling';

export const panelHeaderVariants = tv({
  base: [
    'flex flex-wrap items-center gap-2',
    'border-b border-border bg-surface-2',
    'px-3.5 py-2.5',
    'font-display text-lg font-medium text-text leading-tight tracking-tight',
  ],
});
