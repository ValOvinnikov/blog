/** Fixed document ids — both new, no prior document to derive them from. */
export const TAXONOMY_LIST_TAGS_ID = 'taxonomyList-tags';

/** Matches the singleton's Studio document id (`documentId(tagIndexPageSchema.name)` in `sanity.config.ts`). */
export const PAGE_TAG_INDEX_ID = 'page_tagIndex';
