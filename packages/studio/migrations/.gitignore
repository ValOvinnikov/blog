# Dataset export backups (see README.md). These are large tarballs that may
# contain real content — never commit them.
backups/*
!backups/.gitkeep
*.tar.gz

# Local "current migration" pointer used by scripts/migrate.mjs (per-developer).
.current
