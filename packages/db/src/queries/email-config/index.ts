// Barrel for the email-config query domain — one folder per query,
// re-exported here as the `emailConfig` namespace (see ../index.ts).
export * from './get-email-config';
export * from './upsert-email-config';
