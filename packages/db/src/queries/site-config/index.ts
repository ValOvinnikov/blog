// Barrel for the site-config query domain — one folder per query, re-exported
// here as the `siteConfig` namespace (see ../index.ts).
export * from './get-site-config';
export * from './upsert-site-config';
