{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "@blog/tsconfig/base.json",
  "compilerOptions": {
    "noEmit": true,
    "types": ["node", "vitest/globals"],
    "paths": {
      "@blog/auth/*": ["./src/*"],
      "@blog/db/*": ["../../packages/db/src/*"],
      "@blog/config/*": ["../../packages/config/src/*"],
      "@blog/email/*": ["../../packages/email/src/*"],
      // Not imported directly by this package's own source either — needed
      // only because @blog/email/* resolves to source, and one of its
      // html-shell modules imports @blog/utils/color.
      "@blog/utils/*": ["../../packages/utils/src/*"]
    }
  },
  "include": ["src"]
}
