import { tv } from '@platform/utils/tv/tv';

export const findingDetailsVariants = tv({
  slots: {
    pre: [
      'overflow-x-auto rounded-lg bg-admin-surface-2 p-2.5',
      'text-[12px] text-admin-muted',
    ],
  },
});
