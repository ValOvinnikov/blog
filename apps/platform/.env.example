# Env vars for apps/platform. Next only loads env files from this app's own
# directory, so apps/web/.env.local does NOT reach this app — copy this file
# to apps/platform/.env.local and fill it in even if apps/web is already set up.
#
# The six AUTH_*/MAGIC_LINK_FROM_ADDRESS vars below are declared (with Zod
# validation) in @blog/auth, not in this app's own env module — but this app
# runs its own Node process, and @blog/auth reads whichever process.env
# loaded it. Extracting the shared auth package moved where these are
# *declared*, not where they're *needed*: they still have to be set here.

# Required. Signs/encrypts Auth.js session cookies.
# MUST be byte-identical to apps/web's AUTH_SECRET — the two apps share one
# Auth.js session (see docs/context/environment-variables.md), and a mismatch
# doesn't error, it just makes each app silently reject the other's session
# cookie, so shared sign-in stops working with no visible cause.
AUTH_SECRET=

# Optional OAuth provider credentials — feature-flag-by-absence. Omitting a
# pair disables just that provider's sign-in button; the other methods are
# unaffected.
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Optional. "From" address for Auth.js magic-link email. Unset falls back to
# Resend's shared testing sender — fine for local dev.
MAGIC_LINK_FROM_ADDRESS=

# Required. Pooled Neon connection string (drizzle-orm/neon-http), read by
# @blog/db — first consumed here by the Auth.js Drizzle adapter.
DATABASE_URL=

# Optional. Resend API key used to actually send the magic-link email via
# this app's sendEmail (@platform/server/email/send-email). Absent: every other
# sign-in method still works, but magic-link sign-in fails at send time.
RESEND_API_KEY=

# Optional. Read-write token for the Vercel Blob store the Look tab's
# logo/favicon uploads write to. Absent: the upload/clear actions return a
# readable "uploads aren't configured" error instead of failing silently.
BLOB_READ_WRITE_TOKEN=

# Optional. Base URL of the apps/web deployment this app's Look/Voice saves
# call after a successful site_config write, to revalidate apps/web's cache
# on-demand instead of waiting out its 3600s fallback window. Absent: the
# call is skipped and logged, the save itself still succeeds.
WEB_APP_URL=

# Optional. Bearer secret sent to apps/web's /api/revalidate-site-config
# route above. MUST be byte-identical to apps/web's own
# SITE_CONFIG_REVALIDATE_SECRET — same posture as AUTH_SECRET — or the call
# is rejected with 401 (logged, the save itself still succeeds).
SITE_CONFIG_REVALIDATE_SECRET=

# Optional, local dev only. When set, forwarded as the adminAppBaseUrl
# workflow_dispatch input on provision-tenant.yml runs, so the full
# provisioning flow (Sanity CORS origin, status callbacks) can be tested
# against a locally-tunneled apps/platform (e.g. a Tailscale funnel URL) instead
# of the real production admin app. NEVER set this in the production Vercel
# project — it would silently redirect a real tenant's Sanity CORS origin and
# provisioning status callbacks to whatever URL is set here.
TENANT_PROVISIONING_ADMIN_BASE_URL_OVERRIDE=

# Optional. Which Sanity dataset (development/production) a new tenant's
# project gets created in, and the environment forwarded to
# provision-tenant.yml/deprovision-tenant.yml. Absent: CI falls back to the
# TENANT_SANITY_DATASET GitHub Environment default.
TENANT_PROVISIONING_DATASET=

# Optional. The "owner/repo" this app dispatches
# provision-tenant.yml/deprovision-tenant.yml against via workflow_dispatch.
# Paired with TENANT_PROVISIONING_GITHUB_TOKEN below — absent, the dispatch
# is skipped and logged.
TENANT_PROVISIONING_GITHUB_REPO=

# Optional. A narrowly-scoped (actions: write only) GitHub PAT used to
# trigger provision-tenant.yml (the "Add tenant" wizard) and
# deprovision-tenant.yml (the tenant status page's "Deprovision tenant"
# control). Absent: the dispatch is skipped and logged; the triggering
# action still succeeds so an operator can retry once this is configured.
TENANT_PROVISIONING_GITHUB_TOKEN=

# Optional. Read-scoped Vercel API token the tenant status page uses to
# check a custom domain's live DNS verification state (Vercel's Domains
# API). Absent: the status page shows "not configured" instead of a live
# check.
VERCEL_API_TOKEN=

# Optional. The shared apps/web Vercel project id — every tenant's custom
# domain is added to this one project, so domain verification is checked
# against it. Absent: same as VERCEL_API_TOKEN being unset.
VERCEL_PROJECT_ID_WEB=

# Optional, only needed when the Vercel account is team-owned (its API
# requires a team id as a query param in that case). A personal-account
# token needs no team id even when VERCEL_API_TOKEN is set.
VERCEL_TEAM_ID=
