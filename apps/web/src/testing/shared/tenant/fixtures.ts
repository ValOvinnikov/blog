/**
 * Stub Sanity project/dataset pair for stories that need to resolve a
 * `getSanityImageBaseUrl` call but have no real tenant to fetch one from.
 */
export const STUB_IMAGE_TENANT = {
  projectId: 'storybook-project',
  dataset: 'storybook-dataset',
};
