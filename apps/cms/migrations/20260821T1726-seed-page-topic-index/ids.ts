/** Fixed document ids — both new, no prior document to derive them from. */
export const TAXONOMY_LIST_TOPICS_ID = 'taxonomyList-topics';

/** Matches the singleton's Studio document id (`documentId(topicIndexPageSchema.name)` in `sanity.config.ts`). */
export const PAGE_TOPIC_INDEX_ID = 'page_topicIndex';
