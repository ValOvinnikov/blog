/** The slug-free `/dashboard` tree's "which tenant is active" cookie — set by `/api/dashboard/select-tenant`, read by `resolveDashboardTenant`. */
export const ACTIVE_TENANT_COOKIE = 'admin-active-tenant';
