import { tv } from '@admin/utils/tv/tv';

export const dashboardTenantPickerVariants = tv({
  base: [
    'mx-auto flex min-h-dvh max-w-sm flex-col justify-center gap-4 px-4',
    'bg-admin-bg text-admin-text',
  ],
});
