{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm exec turbo run storybook:build --filter=web && rm -rf storybook-static && cp -R apps/web/storybook-static storybook-static",
  "outputDirectory": "storybook-static",
  "ignoreCommand": "sh \"$(git rev-parse --show-toplevel)/scripts/vercel-ignore-affected.sh\" web"
}
